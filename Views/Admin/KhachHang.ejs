<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>SoundWorld</title>
    <!-- Favicon icon -->
    <link rel="icon" type="image/png" sizes="16x16" href="/Images/Home/logo.png">
    <!-- Custom Stylesheet -->
    <link href="/plugins/tables/css/datatable/dataTables.bootstrap4.min.css" rel="stylesheet">
    <link href="/Css/admin.css" rel="stylesheet">
    </script> -->
    <style>
        .empty {
            color: rgb(190, 187, 187);
        }
    </style>
</head>

<body>
    <%- include("layouts/header") -%>
    <div class="content-body">
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                <% if(messageDelete!=''){%>
                    <div class="alert success" id="success-alert" style="text-align: center; width: 800px;margin-left: 150px;">
                        <span class="closebtn">&times;</span>
                        <strong><%= messageDelete%></strong>
                    </div>
                <%}%>
                    <div class="card">
                        <div class="card-body">
                            <h4 class="card-title">Danh sách khách hàng <small>(<%= thanhvien.length %> khách hàng)</small></h4>
                            <div class="table-responsive">
                                <table class="table table-striped table-bordered zero-configuration">
                                    <thead>
                                        <tr>
                                            <th>Họ tên</th>
                                            <th>Email</th>
                                            <th>Số điện thoại</th>
                                            <th>Giới tính</th>
                                            <th>Ngày sinh</th>
                                            <th>Địa chỉ</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% thanhvien.forEach(function(data){  %>
                                            <tr>
                                                <td>
                                                    <%= data.hoten %>
                                                </td>
                                                <td>
                                                    <%= data.email %>
                                                </td>
                                                <td>
                                                    <% if(data.informationuser){ %>
                                                        <%= data.informationuser.phone %>
                                                            <%}else{%><i class="empty">{Chưa cập nhật}</i>
                                                                <%}%>
                                                </td>
                                                <td>
                                                    <% if(data.informationuser){ %>
                                                        <%= data.informationuser.gender %>
                                                            <%}else{%><i class="empty">{Chưa cập nhật}</i>
                                                                <%}%>
                                                </td>
                                                <td>
                                                    <% if(data.informationuser){ %>
                                                        <%= data.informationuser.birthday %>
                                                            <%}else{%><i class="empty">{Chưa cập nhật}</i>
                                                                <%}%>
                                                </td>
                                                <td>
                                                    <% if(data.informationuser){ %>
                                                        <%= data.informationuser.address %>
                                                            <%}else{%><i class="empty">{Chưa cập nhật}</i>
                                                                <%}%>
                                                </td>
                                            </tr>
                                            
                                            <%})%>
                                    </tbody>
                                    <tfoot>
                                        <tr>
                                            <th>Họ tên</th>
                                            <th>Email</th>
                                            <th>Số điện thoại</th>
                                            <th>Giới tính</th>
                                            <th>Ngày sinh</th>
                                            <th>Địa chỉ</th>
                                        </tr>
                                    </tfoot>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include("layouts/footer") -%>
    </div>
    <script src="/js/admin/close-alert.js"></script>
    <%- include("layouts/script") -%>

</body>

</html>